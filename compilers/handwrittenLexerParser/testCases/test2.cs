include "av"
include "bd"

int abc ()
{
	if (o <= q)
		u = l;
}

int main ()
{
	if (a == b)
	{
		d = e += c;
		for (i = j; a > b; m = n + k;)
			g = h;
	}
	else
	{
		x = y;
		if (w > r)
		{
			t = s;
		}
		
	}
}
